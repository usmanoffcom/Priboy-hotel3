#!/bin/sh
# Убирает служебные хвосты IDE из текста коммита (перед финализацией).
msg_file=$1
[ -n "$msg_file" ] && [ -f "$msg_file" ] || exit 0
tmp="${msg_file}.tmp"
grep -v '^Made-with: Cursor$' "$msg_file" > "$tmp" && mv "$tmp" "$msg_file"
